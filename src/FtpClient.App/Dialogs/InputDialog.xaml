<Window x:Class="FtpClient.App.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" FontSize="16" FontWeight="SemiBold"/>
        <Border Grid.Row="1"
                Margin="0,14,0,14"
                BorderBrush="{StaticResource PrimaryBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="White">
            <TextBox x:Name="ValueInput"
                     BorderThickness="0"
                     Background="Transparent"/>
        </Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button MinWidth="76"
                    Style="{StaticResource SecondaryButton}"
                    IsCancel="True">
                <TextBlock Text="取消" Foreground="{StaticResource TextBrush}"/>
            </Button>
            <Button MinWidth="76"
                    IsDefault="True"
                    Click="Confirm_OnClick">
                <TextBlock Text="确定" Foreground="White"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>
