<Window x:Class="FtpClient.App.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        Height="190"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" FontSize="16" FontWeight="SemiBold"/>
        <TextBox x:Name="ValueInput" Grid.Row="1" Margin="0,14,0,14"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消"
                    MinWidth="76"
                    Style="{StaticResource SecondaryButton}"
                    IsCancel="True"/>
            <Button Content="确定"
                    MinWidth="76"
                    IsDefault="True"
                    Click="Confirm_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
